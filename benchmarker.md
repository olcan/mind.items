#benchmarker benchmarks items that define `_benchmark_*()`. Benchmarks are run automatically on changes but can also be run manually as `/benchmark [items]`.  
<< command_table() >>

```js_removed:benchmarker.js
// benchmarker.js
```

#_listen #_util/core