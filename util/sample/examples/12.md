#util/sample/examples/12 
```js:js_input

const ph = sample(between(0,1)) // p(heads)
const h0 = sample(integer(0,5)) // #heads at t0
const x0 = { t:0, flips:0, heads:h0, awake:true, /* _events:[], _states:[] */ }
const xt = sim(x0, 3, {
  sleep: at_(hour(3), toggle('awake', false)),
  wake:  at_(hours(9,10), toggle('awake', true)),
  flip:  do_(inc(x=>({ flips:1, heads:random_boolean(ph) }))),
})
// print_history(x3)
confine(xt.heads, 6)

```
```js:js_removed

const _sample_options = { 
  stats:'mks ess wsum r t',
  max_time: 5000,
  plot:true,
  // size:1,
}

```
#_util
