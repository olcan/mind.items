#util/sample/examples/1 #_util
```js_input
sample(() => {
  let a = sample(uniform())
  let b = sample(uniform(a, 1))
  condition(b > .9)
  return b
}, {stats:'tks', log:true, plot:true, targets, min_updates:10 })
```
```_output
0.9260332129865358
```
```js_removed
const post_a = random_array(1000, () => {
  const a = random()
  const b = random_uniform(a, 1)
  if (b > .9) return a
}, defined)
const targets = {
  a: post_a,
  b: random_array(1000, ()=>random_uniform(random(), 1), x=>x>.9)
}
```
#/stats #/hist_a #/hist_b
```_log
mks_buffer: 1000 (period 3)
[0] a = sample(uniform()) --> target sample size=1000, mean=0.708
[1] b = sample(uniform(a, 1)) --> target sample size=1000, mean=0.956
sampled 1000 prior runs (ess 1000) in 3ms
ess 325 (essu 1000) for posterior@u=0
reached target ess=1000≥500, mks=2.533≤3, mlw=0≤0 @ u=10, t=37ms
applied 10 updates in 31ms
ess 1000 (essu 1000) for posterior@u=10
{reweights:0,resamples:1,moves:36,proposals:36,000,accepts:11,981,sample_time:3,reweight_time:0,resample_time:1,move_time:17,mks_time:1,tks_time:12}
```