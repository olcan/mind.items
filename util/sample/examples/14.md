#util/sample/examples/14
```js:js_input

let x = { t: t0, awake: false }
simulate(x, t0 + 10, events)

```
```js:js_removed

// TODO: infer sleep/wake parameters by confining to a "distribution" of observed (and fixed) sleep/wake times, using a special domain that uses ks2_density w/ appropriate pre-processing to save time.

const wake_times = event_log_times('wake')
const sleep_times = event_log_times('sleep')
const t0 = last_hour(6, wake_times[0] ?? event_time())

const awake = x => x.awake
const asleep = x => !x.awake
const sleep = ft => _if(awake, ft, x=>x.awake=false)
const wake = ft => _if(asleep, ft, x=>x.awake=true)
const events = name_events(
  () => wake(times(wake_times)),
  () => sleep(times(sleep_times))
)

const _sample_options = { 
  stats:'mks ess wsum r t',
  max_time:5000,
  table:true,
  // plot:true,
  size:1, // debug
}

```
#_util #_logger