#util/sample/examples/13
```js:js_input

let x = { t:0, prices:array(A,1), alloc:array(A), value:1 }
sample_array(A, x.alloc, summand(1), 'crypto stocks bonds reits')
simulate(x, 10, reprice)
confine(x.value, or(below(1), above(2)))
plot(x.value)

```
```js:js_removed

const A = 4 // assets
const μA = [.05, .025, .005, .0125] // mean change (relative) per period
const σA = [.1, .05, .01, .025] // standard deviation in change per period
const dA = array(A, a=> μA[a]-.5*σA[a]**2)
const reprice = name(_do(x=>{
  apply(x.prices, (x,a)=> x * exp(dA[a] + σA[a]*random_normal()))
  x.value = sum(A, a=> x.alloc[a] * x.prices[a])
}), 'reprice')

const _sample_options = { 
  params:{A},
  stats:'mks ess wsum r t',
  max_time: 10000,
  table:true,
  plot:true,
  // log:true,
  // size:1, // debug
}

```
#_util
