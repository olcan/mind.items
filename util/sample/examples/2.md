#util/sample/examples/2 #_util
```js_input
sample(() => {
  let a = sample(uniform())
  let b = sample(uniform(a, 1))
  weight(log(clip((1-.9)/(1-a)))) // log_p(b>.9|a) ≡ past-conditioning
  return b
}, {stats:'tks', log:true, plot:true, targets, min_updates:10 })
```
```_output
0.20034349673047414
```
```js_removed
const post_a = random_array(1000, () => {
  const a = random()
  const b = random_uniform(a, 1)
  if (b > .9) return a
}, defined)
const targets = {
  a: post_a,
  b: post_a.map(a=>random_uniform(a, 1))
}
```
#/stats #/hist_a #/hist_b
```_log
mks_buffer: 1000 (period 3)
[0] a = sample(uniform()) --> target sample size=1000, mean=0.71
[1] b = sample(uniform(a, 1)) --> target sample size=1000, mean=0.86
sampled 1000 prior runs (ess 1000) in 3ms
ess 936 (essu 1000) for posterior@u=0
reached target ess=1000≥500, mks=0.339≤3, mlw=-16.104≤0 @ u=10, t=32ms
applied 10 updates in 26ms
ess 1000 (essu 1000) for posterior@u=10
{reweights:2,resamples:2,moves:20,proposals:20,000,accepts:11,721,sample_time:3,reweight_time:0,resample_time:1,move_time:10,mks_time:0,tks_time:12}
```