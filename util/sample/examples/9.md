#util/sample/examples/9 Sample from various #///domains using implicit samplers.
```js:js_input

let u = sample(between(0, 1)) // uniform(0, 1)
let t = sample(between(0, 1, {mode:.5})) // triangular(0, 1, .5)
let t0 = sample(between(1, 2, {mode:1})) // triangular(1, 2, 1)
let t1 = sample(between(-3, -2, {mode:-2})) // triangular(-3, -2, -2)
let b = sample(between(1, 2, {mean:7/5, stdev:1/5})) // beta(1, 2, 2, 3)
let bc = sample(between(1, 2, {mode:4/3, stdev:1/5})) // beta(1, 2, 2, 3)
let n = sample(around(0, 1)) // normal(0, 1)
let n11 = sample(around(1, 1)) // normal(1, 1)
let n12 = sample(around(1, 2)) // normal(1, 2)
let e = sample(above(0, 1, 1)) // gamma(0, 1, 1)
let g4 = sample(above(0, 1, .5)) // gamma(0, 1, .5)
let g4b1 = sample(above(1, 2, .5)) // gamma(1, 2, .5)
let g4b1c = sample(above(1, 7/4, .5, 'mode')) // gamma(1, 2, .5)
let g16 = sample(above(0, 2, .5)) // gamma(0, 2, .5)
let g16b1 = sample(above(1, 3, .5)) // gamma(1, 3, .5)
let g16n = sample(below(0, -2, .5)) // gamma(0, -2, .5)
let g16nb1 = sample(below(1, -1, .5)) // gamma(1, -1, .5)
let g16nb1c = sample(below(1, -7/8, .5, 'mode')) // gamma(1, -1, .5)

```
```js:js_removed

const targets = transpose_objects(random_array(1000, () => ({
  u: random_uniform(0, 1),
  t: random_triangular(0, 1, .5),
  t0: random_triangular(1, 2, 1),
  t1: random_triangular(-3, -2, -2),
  b: 1+random_beta(2, 3),
  bc: 1+random_beta(2, 3),
  n: random_normal(),
  n11: 1 + random_normal(),
  n12: 1 + random_normal() * 2,
  e: random_exponential(),
  g4: random_gamma(4) / 4, // ɑ=(μ/σ)^2, θ=μ/ɑ=σ^2/μ
  g4b1: 1 + random_gamma(4) / 4,
  g4b1c: 1 + random_gamma(4) / 4,
  g16: random_gamma(16) / 8,
  g16b1: 1 + random_gamma(16) / 8,
  g16n: -random_gamma(16) / 8,
  g16nb1: 1 - random_gamma(16) / 8,
  g16nb1c: 1 - random_gamma(16) / 8,
})))

const _sample_options = {
  targets,
  max_time:5000,
  stats:'mks tks ess',
  plot:true,
}

```
#_util