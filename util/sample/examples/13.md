#util/sample/examples/13
```js:js_input

const pA = sample_array(4, summand(1), 
  { names:['crypto','stocks','bonds','reits'] })
const x0 = { t: 0, prices:array(A,1), value:1 }
const xt = simulate(x0, 10, () => reprice(pA))
// confine(xt.value, above(2))
plot(xt.value)

```
```js:js_removed

const A = 4
const σA = [1, .25, .05, .15]
// TODO: figure out how to get the geometric brownian motion martingale!
const reprice = pA => _do(x=>{
  apply(x.prices, (x,a)=> x * exp(σA[a]*random_normal()-σA[a]*σA[a]/2))
  x.value = sum(A, a=> pA[a] * x.prices[a])
})

const _sample_options = { 
  stats:'mks ess wsum r t',
  max_time: 5000,
  table:true,
  plot:true,
  // log:true,
  // size:1, // debug
}

```
#_util
