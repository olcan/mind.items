#util/sample/examples/13 Optimize [simulation](#util/sim) outcomes under constraints.
```js:js_input

let alloc = array(A)
let [crypto, stocks, bonds, reits] = sample_array(A, alloc, summand(1))
let x = state({ prices:ones(A), value:1 }, { alloc })
simulate(x, 10, events)
confine(bonds + reits, above(.3))
maximize(x.value)

```
```js:js_removed

// before _State: pps is ~220k, aps ~120k
// _State w/o tracking: ~70%
// _State w/o tracking, w/ non-reused frozen alloc array: ~60%
// _State w/ tracking: ~55% (minimal overhead since no dependencies)
// _State w/ proxy-based tracking of array variables: ~15%
//   (restricted to underscore-prefixed keys at top level)

const A = 4 // assets
const μA = [.05, .025, .005, .0125] // mean change (relative) per period
                                    // crypto (0) dominates, more at higher q
// const μA = [.04, .05, .05, .05] // <- 0 @q=.9, 1 @q=.8
// const μA = [.05, .05, .05, .05] // <- 0 @q>.7, 3 @q<.4
const σA = [.1, .05, .01, .025] // standard deviation in change per period
const dA = array(A, a=> μA[a]-.5*σA[a]**2)
const reprice = _do(x=>{
  apply(x.prices, (x,a)=> x * exp(dA[a] + σA[a]*random_normal()))
  x.value = sum(A, a=> x.alloc[a] * x.prices[a])
})
const events = name_events({ reprice })

const _sample_options = { 
  params:{A},
  stats:'mks ess lwr r.0 r.x.value t ua.crypto median.x.value median.bonds median.reits',
  max_time: 10000,
  // max_time: 10000,
  // min_time: 9000, // to see degradation w/o reweights (if min_ess > 1-q)
  // opt_time: 3000,
  // min_ess: 900,
  table:true,
  plot:true,
  // log:true,
  // size:1, // debug
}

```
#_util
