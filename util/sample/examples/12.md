#util/sample/examples/12 
```js:js_input

const ph = sample(between(0,1)) // p(heads)
const h0 = sample(integer(0,5)) // #heads at t0
const x0 = state({flips:0, heads:h0, awake:true})//, 0, 0)
const xt = sim(x0, 3, {
  sleep: if_(x=>x.awake, hour(3), x=>x.awake=false),
  wake:  if_(x=>!x.awake, hours(9,10), x=>x.awake=true),
  flip:  do_(inc(x=>({ flips:1, heads:random_boolean(ph) }))),
})
// print_history(xt)
confine(xt.heads, 6)

```
```js:js_removed

const _sample_options = { 
  stats:'mks ess wsum r t',
  max_time: 5000,
  plot:true,
  // size:1,
}

```
#_util
