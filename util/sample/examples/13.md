#util/sample/examples/13
```js:js_input

// define initial state x0, including unknowns sampled above
const [p_crypto, p_stocks, p_bonds, p_reits] = sample_array(4, summand(1))
const x0 = { t: 0, crypto:1, stocks:1, bonds:1, reits:1 }

// simulate final state xt
// const xt = simulate(x0, 10, () => reprice)

```
```js:js_removed

const reprice = _do(x=>{
  x.crypto *= exp(random_normal()*1)
  x.stocks *= exp(random_normal()*.5)
  x.bonds *= exp(random_normal()*.1)
  x.reits *= exp(random_normal()*.2)
})

const _sample_options = { 
  stats:'mks ess wsum r t',
  max_time: 1000,
  plot:true,
  log:true,
  size:1, // debug
}

```
#_util
