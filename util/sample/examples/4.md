#util/sample/examples/4
```js:js_input

sample(() => {
  let a = sample(uniform(0, 1)) // uniform prior P(A) on [0,1]
  let b = sample(uniform(a, 1)) // uniform prior P(B|a) on [a,1]
  let c = sample(uniform(a, b)) // uniform prior P(C|a,b) on [a,b]
  let c_forked = sample(uniform(a, b))
  condition(c_forked > .45 && c_forked < .55)
}, {stats:'tks ess elw', plot:true, targets })

```
```js:js_removed

// TODO: structure this better and use in other examples also

const post_ab = random_array(1000, () => {
  const a = random()
  const b = random_uniform(a, 1)
  const c = random_uniform(a, b)
  if (c > .45 && c < .55) return [a,b]
}, defined) // calls function until it returns

const post_c = post_ab.map(([a,b])=>random_uniform(a, b))

const post_c_forked = random_array(1000, ()=>{
  const a = random()
  const b = random_uniform(a, 1)
  return random_uniform(a, b)
}, c => c > .45 && c < .55)

const targets = {
  a: post_ab.map(first),
  b: post_ab.map(last),
  c: post_c,
  c_forked: post_c_forked
}

```
#_util