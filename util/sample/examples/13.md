#util/sample/examples/13
```js:js_input

const pA = sample_array(4, summand(1))
const x0 = { t: 0, prices:array(A,1), value:1 }
const xt = simulate(x0, 10, () => reprice(pA))
confine(xt.value, above(20))
// TODO: option 'table' to print out table of basic stats! see event stats table!
// then(...) ??

```
```js:js_removed

const A = 4
const σA = [1,.5,.1,.2]
const reprice = pA => _do(x=>{
  mul(x.prices, array(A, a=> σA[a] * exp(random_normal())))
  x.value = dot(pA, x.prices)
})

const _sample_options = { 
  stats:'mks ess wsum r t',
  max_time: 5000,
  plot:true,
  // log:true,
  // size:1, // debug
}

```
#_util
