#util/sample/examples/13
```js:js_input

// TODO: fix issues with plotting constants before/after confine
//       before confine all are identical, after you have undefined
// TODO: handle destructuring (flat full array) assignments in parser

// sample(constant(0))
const x = sample_array(2, summand(1))
confine(x[0], below(.1))
sample(constant(0))

```
```js:js_removed

const _sample_options = { 
  stats:'mks ess wsum r t',
  max_time: 1000,
  plot:true,
  log:true,
  // size:1, // debug
}

```
#_util
